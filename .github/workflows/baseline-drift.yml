name: Baseline Drift

on:
  schedule:
    - cron: '9 4 * * 1' # weekly, Monday
  workflow_dispatch:

permissions:
  contents: read

jobs:
  drift:
    uses: BAGArt/telegram-platform-workflows/.github/workflows/baseline-drift.yml@36856d23c1100f2f6f5ca207ecf018662c57fb1e # pin!
