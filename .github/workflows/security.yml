name: Security

on:
  push:
    branches:
      - develop
      - main
      - master
      - workos
  pull_request:
  schedule:
    # Weekly dependency/secret sweep — cheap, no container builds (cost guard).
    - cron: '23 4 * * 1'

permissions:
  contents: read

jobs:
  security:
    uses: BAGArt/telegram-platform-workflows/.github/workflows/security.yml@262b6fbf75e2d7c0262b13a79c0cb7b8393fac05 # pin!
    permissions:
      contents: read
      security-events: write # SARIF upload when export-sarif is enabled
    with:
      php-version: '8.5'
      export-sarif: true
