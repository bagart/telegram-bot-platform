name: Nightly

on:
  schedule:
    # Nightly deep sweep: full suite + audits (legacy host schedule).
    - cron: '17 2 * * *'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  nightly:
    uses: BAGArt/telegram-platform-workflows/.github/workflows/nightly.yml@36856d23c1100f2f6f5ca207ecf018662c57fb1e # pin!
    with:
      php-version: '8.5'
