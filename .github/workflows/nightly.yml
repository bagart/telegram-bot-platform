name: Nightly

on:
  schedule:
    # Nightly deep sweep: full suite + audits (legacy host schedule).
    - cron: '17 2 * * *'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  nightly:
    uses: BAGArt/telegram-platform-workflows/.github/workflows/nightly.yml@9d363fa8e3f6578105b0b3d37bb50713a4f7d58f # pin!
    with:
      php-version: '8.5'
