name: Nightly

on:
  schedule:
    # Nightly deep sweep: full suite + audits (legacy host schedule).
    - cron: '17 2 * * *'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  nightly:
    uses: BAGArt/telegram-platform-workflows/.github/workflows/nightly.yml@d846c25539617e8fd9b24355f19a326951bc895e # pin!
    with:
      php-version: '8.5'
