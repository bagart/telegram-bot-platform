name: Nightly

on:
  schedule:
    # Nightly deep sweep: full suite + audits (legacy host schedule).
    - cron: '17 2 * * *'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  nightly:
    uses: BAGArt/telegram-platform-workflows/.github/workflows/nightly.yml@eadb81c9ba237ba15e1c827d69f8f5c837cec051 # pin!
    with:
      php-version: '8.5'
